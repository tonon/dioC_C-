#ifndef CONVERT_H
#define CONVERT_H

char* decToBin(int n);
char* decToOct(int n);
char* decToHex(int n);
int binToDec(const char* bin);
int octToDec(const char* oct);
int hexToDec(const char* hex);

#endif // CONVERT_H
